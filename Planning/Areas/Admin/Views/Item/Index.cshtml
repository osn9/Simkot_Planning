@model Planning.Areas.Admin.Models.ItemViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    int i = 1;

    //List<Planning.Planning.Entities.Sarta_Setup> sarta = new List<Planning.Planning.Entities.Sarta_Setup>();
    //using (Planning.Planning.Entities.PLANNING_OCT_03Entities db = new Planning.Planning.Entities.PLANNING_OCT_03Entities())
    //{
    //    sarta = (from j in db.Sarta_Setup select j).ToList();
    //}

}

<script src="~/ckeditor_4.11.1_standard/ckeditor/ckeditor.js"></script>
<div class="padding-md">

    <div class="row">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="panel panel-default">
                <div class="header-list" style="text-align: center">
                    <h2> परिमाण</h2>
                </div>
                <hr />

                @*@if (sarta.Count() > 0)
                    {
                        <div class="panel-heading">
                        </div>
                    }
                    else
                    {
                        <div class="panel-heading">
                            @Html.ActionLink("नयाँ राख्नुहोस्", "Create", null, new { @class = "btn btn-success btn_add btn-xs" })
                        </div>
                    }*@

                <div class="panel-heading">
                    @Html.ActionLink("नयाँ राख्नुहोस्", "Create", null, new { @class = "btn btn-success btn_add btn-xs" })
                </div>

                <table class="table table-bordered table-condensed table-hover table-striped">
                    <thead>
                        <tr class="success">
                            <th>क्र.स.</th>
                            <th>
                                नाम
                            </th>
                            <th>

                                एकाइ
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.list)
                        {
                            <tr>
                                <th>@Planning.Utility.NepaliEnglishNumber.English_Nepali(i.ToString())</th>
                                <td>@Html.Raw(item.name)</td>
                                <td>@Html.Raw(item.ekainame)</td>
                                <td>
                                    <a class="btn btn-success btn-sm" title="Edit" href="@Url.Action("Create", "Item", new { id = item.itemId })"><i class="fa fa-edit"></i> सच्याउनुहोस्</a>
                                    <a class="btn btn-danger btn-sm" title="  हटाउनु" href="@Url.Action("Delete", "Item", new { id = item.itemId })"><i class="fa fa-edit"></i>  हटाउनु </a>
                                </td>
                            </tr>
                            i++;
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>





